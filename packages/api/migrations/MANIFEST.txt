# Runtime migration order.
#
# Only files listed here are executed by core.database.run_migrations().
# Historical or scratch SQL may coexist in this directory locally without
# being applied accidentally. Add every new production migration explicitly.
#
# 000_baseline_v1.sql is the 1.0 baseline: a pg_dump --schema-only snapshot
# taken 2026-07-26 after Trust removal (P2) and migration history
# consolidation (P1). schema_version = 1 (see core/database.py). All prior
# migrations (000_baseline.sql, 054-056, 110-124) are retired — their
# content is folded into this baseline; recover them via git history if
# ever needed. See ws_spec_plan/mem_d74aa763.
000_baseline_v1.sql
001_spec_validity.sql
002_widen_inquiry_paths_embedding.sql
